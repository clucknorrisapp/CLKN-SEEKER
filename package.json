{
  "name": "cluck-norris-school",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "start": "node server.js",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "prep:solana": "node scripts/prep-dist.mjs solana",
    "prep:store": "node scripts/prep-dist.mjs store",
    "build:solana": "export CLKN_TARGET=solana && npm run prep:solana && npx cap sync android && cd android && ./gradlew :app:assembleRelease",
    "build:play": "export CLKN_TARGET=googlePlay && npm run prep:store && npx cap sync android && cd android && ./gradlew :app:bundleRelease -PclknAppId=app.clucknorris.edu -PclknKeystore=keystore.play.properties -PclknWallet=false",
    "build:ios": "export CLKN_TARGET=ios && npm run prep:store && npx cap sync ios",
    "prep:seeker": "CLKN_TARGET=seeker node scripts/prep-dist.mjs store",
    "build:seeker": "export CLKN_TARGET=seeker && npm run prep:seeker && npx cap sync android && cd android && ./gradlew :app:assembleRelease -PclknAppId=app.clucknorris.seeker",
    "prep:seeker-dev": "CLKN_TARGET=seeker node scripts/prep-dist.mjs seeker-dev",
    "build:seeker-dev": "export CLKN_TARGET=seeker && npm run prep:seeker-dev && npx cap sync android && cd android && ./gradlew :app:assembleDebug -PclknAppId=app.clucknorris.seeker.dev",
    "prep:play-dev": "CLKN_TARGET=googlePlay node scripts/prep-dist.mjs play-dev",
    "build:play-dev": "export CLKN_TARGET=googlePlay && npm run prep:play-dev && npx cap sync android && cd android && ./gradlew :app:assembleDebug -PclknAppId=app.clucknorris.edu.dev -PclknWallet=false",
    "prep:ios-dev": "CLKN_TARGET=ios-dev node scripts/prep-dist.mjs ios-dev",
    "build:ios-dev": "node scripts/build-edu-dev-bundle.mjs --ref ${CLKN_REF:-develop} && export CLKN_TARGET=ios-dev CLKN_IOS_DEV=1 CLKN_IOS_DEV_TGZ=$(cat .cache/edu-dev-bundle.txt) && npm run prep:ios-dev && npx cap sync ios"
  },
  "dependencies": {
    "@capacitor/android": "^8.3.4",
    "@capacitor/core": "^8.3.4",
    "@capacitor/ios": "^8.3.4",
    "@dha-team/arbundles": "^1.0.4",
    "@fontsource/oswald": "^5.2.8",
    "@napi-rs/canvas": "^1.0.0",
    "@solana-mobile/wallet-adapter-mobile": "^2.2.8",
    "@solana/spl-token": "^0.4.14",
    "@solana/wallet-adapter-base": "^0.9.27",
    "@solana/wallet-adapter-react": "^0.15.39",
    "@solana/wallet-adapter-react-ui": "^0.9.39",
    "@solana/web3.js": "^1.98.4",
    "axios": "^1.16.1",
    "express": "^4.18.2",
    "lucide-react": "^0.441.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-router-dom": "^6.26.2",
    "zustand": "^4.5.5"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.3.4",
    "@types/react": "^18.3.3",
    "@types/react-dom": "^18.3.0",
    "@vitejs/plugin-react": "^4.3.1",
    "autoprefixer": "^10.4.20",
    "postcss": "^8.4.47",
    "tailwindcss": "^3.4.10",
    "typescript": "^5.5.4",
    "vite": "^5.4.1"
  }
}
